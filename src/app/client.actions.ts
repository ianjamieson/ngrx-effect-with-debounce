import { createAction, props } from '@ngrx/store';

export const loadClients = createAction(
  '[Client] Load Clients'
);

export const networkRequest = createAction(
  '[Client] networkRequest'
);




